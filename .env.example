# Warehouse API (existing)
VITE_API_URL=http://localhost:5000/api

# Accountant service — optional if mounted on the main API at /accountant
# VITE_ACCOUNTANT_API_URL=http://localhost:5100/api/accountant

# Optional — only needed if ACCOUNTANT_API_KEY is set on the accountant service
VITE_ACCOUNTANT_API_KEY=
